<% content_for :head do %>
  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag js_url(:action => "application") %>
<% end %>


<div class="sectionHeader">お問い合わせ</div>

 <p><span class="must">※</span>は必ずご入力ください。</p>

  <%= @inquiry && error_messages_for(:inquiry) %>
  <%= flash[:notice] %>
  <%= flash[:error] %>
  <% form_for :inquiry, :url => {:action => "confirm"} do |f| %>
    <table class="tb2">
      <tr><td>お名前<span class="must">※</span></td></tr>
      <tr>
        <td><%= f.text_field :name, :size => 30 %></td>
      </tr>
      <tr><td>メールアドレス <span class="must">※</span></td></tr>
      <tr>
        <td><%= f.text_field :email, :size => 40 %></td>
      </tr>
      <tr><td>電話番号</td></tr>
      <tr>
        <td><%= f.text_field :tel, :size => 20 %></td>
      </tr>
      <tr><td>注文番号</td></tr>
      <tr>
        <td><%= f.text_field :order_number, :size => 30 %></td>
      </tr>
      <tr><td>お問合わせ種別 <span class="must">※</span></td></tr>
      <tr>
        <td><%= f.select :kind, Inquiry.pc_kind_list %></td>
      </tr>
      <tr><td>お問い合わせ内容 <span class="must">※</span></td></tr>
      <tr>
        <td><%= f.text_area :body, :rows => 15 %></td>
        </tr>
      </table>
      <div class="confirmInquiry text">
	        内容をご確認の上、送信ボタンを押してください。<br />
	        ページが表示されるまでにお時間がかかる場合がございます。<br />
	        処理をしている間に「送信」を再度クリックすると二重送信になりますので、<br />
	        送信ボタンは1度だけ押してください。<br />
	   <br />
	        お問合せ内容により回答までにお時間がかかる場合がございます｡予めご了承ください。<br />
	        受付対応時間は平日(年末年始をのぞく)の10:00～18:00までとさせていただきます｡
	   <br />
      </div>
	  <br />
	  <div class="my_btn4">
	  	<%= submit_tag "確認", :class=>"rosy button" %>
	  </div>
  <% end -%>
  <!-- /container -->