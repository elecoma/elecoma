<%= error_messages_for :kiyaku %>

<% fields_for :kiyaku do | f | %>
<p class="req"><span class="pnt">※</span>は必須入力です</p>
<table class="data2" cellspacing="1" cellpadding="0">
  <tr>
    <th>規約タイトル <font class="pnt">※</span></th>
    <td><%= f.text_field :name , :class=>"data_l"%>(上限250文字）</td>
  </tr>
  <tr>
    <th>規約内容 <span class="pnt">※</span></th>
    <td><%= f.text_area :content, :class=>"data_l" %>(上限2000文字）</td>
  </tr>

</table>
<%= f.hidden_field :id %>
<% end %>
