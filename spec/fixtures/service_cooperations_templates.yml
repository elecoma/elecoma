# Read about fixtures at http://ar.rubyonrails.org/classes/Fixtures.html

one:
  template_name: AdvantageSearchTemplate
  service_name: AdvantageSearch
  url_file_name: item_master
  file_type: 0
  encode: 0
  newline_character: 2
  sql: SELECT id,title,text,url FROM products WHERE id > 5 ORDER BY id
  field_items: id,title,text,url
  description: AdvantageSearchTemplate

two:
  template_name: RecommendServiceTemplate
  service_name: RecommendService
  url_file_name: reco.csv
  file_type: 0
  encode: 0
  newline_character: 2
  sql: SELECT id,title,text,url FROM products WHERE id > 5 ORDER BY id
  field_items: id,title,text,url
  description:

nils:
  template_name: nils_template
  service_name:
  url_file_name:
  file_type:
  encode:
  newline_character:
  sql:
  field_items:

test:
  template_name: test
  service_name: test
  url_file_name: test
  file_type: 0
  encode: 0
  newline_character: 1
  sql: SELECT name,id FROM products WHERE id < 3 ORDER BY id
  field_items: name,id
